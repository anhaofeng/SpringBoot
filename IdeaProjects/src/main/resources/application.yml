spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/IdeaDemo1
    password:
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

